import { LinkSectionPipe } from './link-section.pipe';

describe('LinkSectionPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkSectionPipe();
    expect(pipe).toBeTruthy();
  });
});
